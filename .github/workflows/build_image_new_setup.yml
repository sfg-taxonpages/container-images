name: Build image (new-setup)
on:
  workflow_dispatch:
    inputs:
      repo:
        type: choice
        description: Repository
        options:
        - aphid
        - chrysididae
        - cockroach
        - coleorrhyncha
        - coreoidea
        - dermaptera
        - embioptera
        - grylloblattodea
        - hoppers
        - isoptera
        - lygaeoidea
        - mantodea
        - mantophasmatodea
        - orthoptera
        - phasmida
        - plecoptera
        - psocodea
        - zoraptera
      branch:
        type: string
        description: Git branch to build
        default: new-setup

env:
  REGISTRY: ghcr.io
  
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
    - name: Git checkout
      uses: actions/checkout@v6
    
    - name: Build image
      run: ./build_image_new_setup.sh ${{ env.REGISTRY }} ${{ github.event.inputs.repo }} ${{ github.event.inputs.branch }}

    - name: Log in to the Container registry
      uses: docker/login-action@v4
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Push image to registry
      run: docker push ${{ env.REGISTRY }}/sfg-taxonpages/${{ github.event.inputs.repo }}
